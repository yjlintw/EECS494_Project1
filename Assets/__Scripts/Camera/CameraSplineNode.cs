using UnityEngine;
using System.Collections;

public class CameraSplineNode : MonoBehaviour {

    public CameraSplineNode nextNode;
    public CameraSplineNode previousNode;
    public float followDistance = 6f;
	// Use this for initialization
}
